format long e
n = 341;
s = rand(1);
B = rand(n);
Col = rand(n,1);
Row = rand(1,n);
sB = sparse(B);
sCol = sparse(Col);
sRow = sparse(Row);

% sparse+scalar, Row+s
tic; sRow+s; toc;
norm(full(Row+s-(Row+s)))
exit